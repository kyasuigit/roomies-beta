import 'package:flutter/material.dart';

class DishTrackScreen extends StatelessWidget {
  const DishTrackScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("DishTrack"),
    );
  }
}
