import 'package:flutter/material.dart';

class FridgeTrackScreen extends StatelessWidget {
  const FridgeTrackScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("FridgeTrack"));
  }
}
