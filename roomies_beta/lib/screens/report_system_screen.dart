import 'package:flutter/material.dart';

class ReportSystemScreen extends StatelessWidget {
  const ReportSystemScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Report"));
  }
}
