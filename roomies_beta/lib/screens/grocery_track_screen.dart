import 'package:flutter/material.dart';

class GroceryTrackScreen extends StatelessWidget {
  const GroceryTrackScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Grocery Track"),
    );
  }
}
